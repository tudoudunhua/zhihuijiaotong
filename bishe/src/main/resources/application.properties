spring.datasource.url=jdbc:mysql://localhost:3306/smart_traffic?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=123456

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=8081

# 违章照片上传相关配置（5MB 单文件，10MB 单次请求）
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ============ 车主通知（短信/电话/留言）===========
# 真实调用建议使用 WEBHOOK 模式，把请求转发给你的短信/语音平台服务
# notification.provider-mode=SIMULATE
# notification.provider-mode=WEBHOOK
# notification.webhook.url=http://127.0.0.1:9000/notify
# notification.webhook.secret=your-shared-secret

# ============ 阿里云 OSS 配置（请按你的实际账号修改） ============

